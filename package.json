{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test --workers=1",
    "test:e2e": "playwright test --workers=1 tests/e2e/e2e.banking-journey.spec.ts",
    "test:api": "playwright test --workers=1 tests/e2e/api.parabank.spec.ts tests/e2e/api.balances.spec.ts",
    "test:component": "playwright test --workers=1 --grep @component",
    "test:negative": "playwright test --workers=1 --grep @negative",
    "test:parallel": "playwright test",
    "create:checking": "bash scripts/create-checking-account.sh",
    "test:ui": "playwright test --workers=1 --ui",
    "test:debug": "playwright test --workers=1 --debug",
    "report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "fast-xml-parser": "^5.5.9"
  }
}
